// TODO: integrate with an AI/LLM API (e.g. OpenAI, Anthropic)

async function sendMessage(userId, message) {
  // TODO: send message to AI service, return response text
}

module.exports = { sendMessage };
