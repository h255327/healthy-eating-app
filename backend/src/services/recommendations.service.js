// const { pool } = require('../config/db');

async function getRecommendations(userId) {
  // TODO: suggest recipes based on user's diet type and meal history
}

module.exports = { getRecommendations };
